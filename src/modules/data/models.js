"use strict";

let BookshelfFactory = require("BookshelfFactory");
let bookshelf = BookshelfFactory.bookshelf;

/*module.exports = {
    Drawing: bookshelf.Model.extend({
        tableName: "drawing"
    })
};*/
